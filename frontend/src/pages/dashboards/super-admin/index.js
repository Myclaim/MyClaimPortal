// Super Admin Dashboard — Barrel Export
// All Super Admin dashboard sub-components should be placed in this folder.
// Import the main component via this index for clean imports elsewhere.

export { default } from './SuperAdminDashboard';
