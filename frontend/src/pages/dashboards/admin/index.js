// Admin Dashboard — Barrel Export
// All Admin dashboard sub-components should be placed in this folder.
// Import the main component via this index for clean imports elsewhere.

export { default } from './AdminDashboard';
